{
  "extends": "./tsconfig.json",  // Extend the simplified main config
  "compilerOptions": {
    "rootDir": ".",  // Ensure rootDir includes both src and tests
    "outDir": "./dist"
  },
  "include": [
    "tests/**/*.ts"   // Keep this specific to test files
  ],
  "exclude": ["node_modules", "dist"]
}
