---
title: useUserDms
---

## Imports

```ts
import { useUserDms } from '@river-build/react-sdk'
```

## Definition

```ts
function useUserDms(
  config?: ObservableConfig.FromObservable<Dms>,
): {
    error: Error | undefined;
    status: "loading" | "loaded" | "error";
    isLoading: boolean;
    isError: boolean;
    isLoaded: boolean;
    streamIds: string[];
} | {
    error: Error | undefined;
    status: "loading" | "loaded" | "error";
    isLoading: boolean;
    isError: boolean;
    isLoaded: boolean;
    streamIds: string[];
} | {
    error: Error | undefined;
    status: "loading" | "loaded" | "error";
    isLoading: boolean;
    isError: boolean;
    isLoaded: boolean;
    streamIds: string[];
}
```

**Source:** [useUserDms](https://github.com/river-build/river/blob/main/packages/react-sdk/src/useUserDms.ts)

## Parameters

### config

- **Type:** `ObservableConfig.FromObservable<Dms>`
- **Optional**

## Return Type

```ts
{
    error: Error | undefined;
    status: "loading" | "loaded" | "error";
    isLoading: boolean;
    isError: boolean;
    isLoaded: boolean;
    streamIds: string[];
} | {
    error: Error | undefined;
    status: "loading" | "loaded" | "error";
    isLoading: boolean;
    isError: boolean;
    isLoaded: boolean;
    streamIds: string[];
} | {
    error: Error | undefined;
    status: "loading" | "loaded" | "error";
    isLoading: boolean;
    isError: boolean;
    isLoaded: boolean;
    streamIds: string[];
}
```
