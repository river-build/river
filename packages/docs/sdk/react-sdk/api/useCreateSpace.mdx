---
title: useCreateSpace
---
Hook to create a space.
## Imports

```ts
import { useCreateSpace } from '@river-build/react-sdk'
```

## Definition

```ts
function useCreateSpace(
  config?: ActionConfig<Spaces['createSpace']>,
): {
    data: {
        spaceId: string;
        defaultChannelId: string;
    } | undefined;
    error: Error | undefined;
    isPending: boolean;
    isSuccess: boolean;
    isError: boolean;
    createSpace: (params: Partial<Omit<import("@river-build/web3").CreateSpaceParams, "spaceName">> & {
        spaceName: string;
    }, signer: import("ethers").Signer) => Promise<{
        spaceId: string;
        defaultChannelId: string;
    }>;
}
```

**Source:** [useCreateSpace](https://github.com/river-build/river/blob/main/packages/react-sdk/src/useCreateSpace.ts)

## Parameters

### config

- **Type:** `ActionConfig<Spaces['createSpace']>`
- **Optional**

Configuration options for the action.

## Return Type

```ts
{
    data: {
        spaceId: string;
        defaultChannelId: string;
    } | undefined;
    error: Error | undefined;
    isPending: boolean;
    isSuccess: boolean;
    isError: boolean;
    createSpace: (params: Partial<Omit<import("@river-build/web3").CreateSpaceParams, "spaceName">> & {
        spaceName: string;
    }, signer: import("ethers").Signer) => Promise<{
        spaceId: string;
        defaultChannelId: string;
    }>;
}
```
